#include <stdio.h>
#include <stdbool.h>
int main(){
    //printf("Hello, world!\n");
    while(1){
        char initialInput[256];
        scanf("%s", initialInput);
        char input[256];
        int j = 0;
        // yasini siktim
        // null terminator must be j?
        // parse input
        

        // produce output

        printf("%s", input);


    }
    return 0;
}
